import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["img.clerk.com", "colorful-boar-98.convex.cloud", "scintillating-snail-353.convex.cloud"],
  },
};

export default nextConfig;
